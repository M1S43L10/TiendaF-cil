const ItemListContainer = ({ mensaje }) => {
  return (
    <div className="container mt-5 text-center">
      <h2 className="display-6">{mensaje}</h2>
    </div>
  );
};

export default ItemListContainer;
